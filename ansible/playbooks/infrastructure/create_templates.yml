---
- name: Create Proxmox Debian 13 Template
  hosts: proxmox
  roles:
    - proxmox_template

  post_tasks:
    - name: "Show Completion Message"
      run_once: true
      ansible.builtin.debug:
        msg:
          - "=================================================="
          - "🏗️  PROXMOX TEMPLATE CREATION COMPLETE"
          - "   • Debian 13 template created successfully"
          - "=================================================="
