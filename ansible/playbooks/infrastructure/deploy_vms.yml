---
- name: Deploy Proxmox VMs
  hosts: proxmox
  roles:
    - proxmox_vm

  post_tasks:
    - name: "Show Completion Message"
      run_once: true
      ansible.builtin.debug:
        msg:
          - "=================================================="
          - "🏗️  PROXMOX VM DEPLOYMENT COMPLETE"
          - "   • VMs cloned and started"
          - "=================================================="
