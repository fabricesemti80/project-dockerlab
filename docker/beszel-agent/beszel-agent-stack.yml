version: "3.9"

services:
  beszel-agent:
    image: henrygd/beszel-agent:latest
    container_name: beszel-agent
    environment:
      - TZ=${TZ}
      - LISTEN=45876
      - "KEY=${BESZEL_SSH_PUBLIC}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /:/host:ro
    ports:
      - "45876:45876"
    restart: unless-stopped
