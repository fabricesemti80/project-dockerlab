version: "3.9"

services:
  grafana:
    image: grafana/grafana:latest
    networks:
      - traefik_traefik-public
      - monitoring-net
    volumes:
      - /mnt/gfs_data/monitoring/grafana-data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    configs:
      - source: grafana_datasources_config
        target: /etc/grafana/provisioning/datasources/datasources.yaml
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.grafana.rule=Host(`grafana.${DOMAIN}`)"
        - "traefik.http.routers.grafana.entrypoints=websecure"
        - "traefik.http.routers.grafana.tls.certresolver=cloudflare"
        - "traefik.http.services.grafana.loadbalancer.server.port=3000"

  prometheus:
    image: prom/prometheus:latest
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
    networks:
      - monitoring-net
      - traefik_traefik-public # Needs access to Traefik network to be exposed
    configs:
      # NOTE: Docker Configs are immutable. If you modify prometheus.yml, you must 
      # rename the config source below (e.g. prometheus_config_v2) to trigger an update.
      - source: prometheus_config
        target: /etc/prometheus/prometheus.yml
    volumes:
      - prometheus-data:/prometheus
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.prometheus.rule=Host(`prometheus.${DOMAIN}`)"
        - "traefik.http.routers.prometheus.entrypoints=websecure"
        - "traefik.http.routers.prometheus.tls.certresolver=cloudflare"
        - "traefik.http.services.prometheus.loadbalancer.server.port=9090"

  node-exporter:
    image: quay.io/prometheus/node-exporter:latest
    command:
      - '--path.rootfs=/host'
    networks:
      - monitoring-net
    volumes:
      - '/:/host:ro,rslave'
    deploy:
      mode: global

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    privileged: true
    networks:
      - monitoring-net
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    devices:
      - /dev/kmsg
    deploy:
      mode: global

configs:
  prometheus_config:
    file: ./prometheus/prometheus.yml
  grafana_datasources_config:
    file: ./grafana/provisioning/datasources.yaml

volumes:
  prometheus-data:

networks:
  traefik_traefik-public:
    external: true
  monitoring-net:
    driver: overlay
    attachable: true
